# dotfiles

WSL2での開発環境をサポートするファイル群を管理しています。

## 🚀 使用方法

```powershell
# PowerShell
wsl --install
```

```bash
# WSL2
git clone [URL]
cd dotfiles
./setup.sh
```
